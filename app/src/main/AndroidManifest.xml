<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joaquin.cerdis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cerdis">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!--android:screenOrientation="portrait"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Debo incluir las activities que vaya creando manualmente -->
        <activity android:name=".Informacion"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".Configuracion"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".CreacionPersonaje" />
        <activity android:name=".CreacionNombre" />
        <activity android:name=".Juego" />
        <activity android:name=".ResultadoPartida" />
    </application>

</manifest>